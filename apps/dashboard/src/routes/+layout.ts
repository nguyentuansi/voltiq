// SPA mode: no SSR, no prerender. The shell is hydrated client-side and fed by
// voltiq's local JSON/SSE API (adapter-static fallback = index.html).
export const ssr = false;
export const prerender = false;
